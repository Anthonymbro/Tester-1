body {
    background-color: lightblue;
}

h1 {
    text-align: center;
    color: red;
    background-color: darkblue;
    margin: 2px;
    border: 3px solid black;
    padding: 20px;
}

ul {
    list-style-type: disc; /* Keeps bullet points */
    list-style-position: inside; /* Moves bullets inside the list items */
    display: flex; /* Aligns items in one row */
    gap: 10px; /* Adds space between items */
    padding: 0;
}


/*This is a comment as a demonstration*/
h2 {
    text-align: center;
    color: red;
    background-color: darkblue;
    border: 3px solid black;
}

img {
    animation-duration: 3s;
    animation-name: spin;
}
 
@keyframes spin {
    0%{
        
    }
    100% {
        transform: (360deg); 
        border-radius: 50%;
    }
}

video {
    border: 3px solid black;
}

table {
    border: 3px solid black;
}

td {
    border: 3px solid black;
}

#opacity {
    font-size: 30px;
    opacity: .4;
    text-shadow: 2px 2px 5px red;
}

/* Code for the flexbox section */

.container

.flex-container {
    display: flex;
    flex-direction: row;
}
